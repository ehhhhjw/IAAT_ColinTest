Appium项目，以我们平台的测试任务为例。下载任务后，将依赖的jar包替换为项目AppiumJavaClient1.3.0.rar。
运行后，操作流文件会自动生成在项目的mooctest文件夹下，以运行时间命名。

这俩项目可能会有一些配置问题，比如他们分别还会依赖一些jar包，都放在文件夹里了，但是具体的引用文件，在你下载后需要改里面的地址位置，根据自己计算机上的位置。主要是改.classpath文件。


此外，对于LIRATReader类里的getWInfoNodeList方法，它会把LIRAT的一次操作信息整合成所需的操作流文件
但是，LIRAT本身对于侧边栏控件的读取性不太好，可能会定位到侧边栏或导航栏后面被挡住的控件上，导致复现失败。
因此在复现的时候需要手动修正。
